<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="rate_approval.list">
      <field name="name">rate_approval list</field>
      <field name="model">rate_approval.rate_approval</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="rate_approval.action_window">
      <field name="name">rate_approval window</field>
      <field name="res_model">rate_approval.rate_approval</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="rate_approval.action_server">
      <field name="name">rate_approval server</field>
      <field name="model_id" ref="model_rate_approval_rate_approval"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="rate_approval" id="rate_approval.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="rate_approval.menu_1" parent="rate_approval.menu_root"/>
    <menuitem name="Menu 2" id="rate_approval.menu_2" parent="rate_approval.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="rate_approval.menu_1_list" parent="rate_approval.menu_1"
              action="rate_approval.action_window"/>
    <menuitem name="Server to list" id="rate_approval" parent="rate_approval.menu_2"
              action="rate_approval.action_server"/>
    -->
  </data>
</odoo>